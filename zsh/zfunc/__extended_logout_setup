setopt IGNORE_EOF
function __extended_logout() {
  echo; echo -n 'logout [y/N]: '
  if read -rq; then
    exit
  else
    zle send-break
  fi
}
zle -N extended-logout __extended_logout
bindkey '^d' extended-logout
